import React, { ReactElement } from 'react'
import MainLayout from '../../components/MainLayout'

interface Props {
  
}

export default function AlbumsPage({}: Props): ReactElement {
  return (
    <MainLayout>
      <h1>Albums !</h1>
    </MainLayout>
  )
}
